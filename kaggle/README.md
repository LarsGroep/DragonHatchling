# kaggle/

Batch GPU notebooks (§2, §3). Thin shells around `packages/core` (`vitreous`)
— one code path, two venues (Kaggle + the HF Space).

Landing at **M4** (§16):

- `train.ipynb` — fine-tune ViT-S/16 per dataset (`DATASET = "eurosat"` is the
  only knob).
- `precompute.ipynb` — curated gallery → Explanation Packs → Supabase.
- `sae.ipynb` — token-activation k-sparse autoencoder → concept dictionary.

Placeholder at M0; no notebooks yet.

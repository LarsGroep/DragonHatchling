"""umtvit.export — interpretability artifacts (ARCHITECTURE §8, §9).

Planned (lands in U6): self-describing exports — the per-image latent cube
(fp16 ``.bin`` + JSON sidecar), SOM component planes / hit maps / U-matrix,
training curves, and the markdown run report. Same self-describing discipline
as the ViTreous packs.

Stub package (U0). No export code is defined yet.
"""

from __future__ import annotations

__all__: list[str] = []
